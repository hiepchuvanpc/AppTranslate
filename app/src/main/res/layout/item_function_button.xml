<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/panel_function_button_size"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:rippleColor="@color/ripple_color"
    app:cardBackgroundColor="@color/surface_variant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="8dp">

        <ImageView
            android:id="@+id/ivFunctionIcon"
            android:layout_width="@dimen/panel_icon_size"
            android:layout_height="@dimen/panel_icon_size"
            android:layout_marginBottom="4dp"
            android:contentDescription="@string/function_icon_description"
            app:tint="@color/primary" />

        <TextView
            android:id="@+id/tvFunctionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="2"
            android:textColor="@color/primary_text"
            android:textSize="@dimen/panel_text_size" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
